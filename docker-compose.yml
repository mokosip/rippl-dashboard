services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: rippl
      POSTGRES_PASSWORD: rippl
      POSTGRES_DB: rippl
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
